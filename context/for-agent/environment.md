# Environment

## Tools
- TypeScript: `tsc` (install via `npm install -g typescript` if needed)
- Git: configured and working
- Node.js + npm: available

## Paths
- Workspace: `/home/eikichi/Agent_Workspace/Yr-Weather-Extension`
- Source code: `/home/eikichi/Agent_Workspace/Yr-Weather-Extension/WeatherApp`
- Build output: same directory (tsc compiles .ts -> .js in place)

## Build Command
```bash
cd /home/eikichi/Agent_Workspace/Yr-Weather-Extension/WeatherApp && npx tsc
```
